// test/HAProfile.test.ts
const { expect } = require('chai');
const { contract, ethers, upgrades } = require('hardhat');

contract("HAProfile Tests", ([alice, bob, carol, david, erin, frank, ...accounts]) => {

});
